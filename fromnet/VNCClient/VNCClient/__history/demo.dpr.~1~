program demo;

uses
  sharemem,
  Forms,
  UDemo in 'UDemo.pas' {viewer_frm},
  logon_frmU in 'logon_frmU.pas' {logon_frm};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tlogon_frm, logon_frm);
  Application.CreateForm(Tviewer_frm, viewer_frm);
  Application.Run;
end.
